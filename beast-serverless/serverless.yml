service: serverless

provider:
  name: aws
  runtime: nodejs6.10
  stage: dev
  region: us-west-2

package:
  exclude:
    - node_modules/aws-sdk/**
    - node_modules/puppeteer/.local-chromium/**

functions:
  takeScreenshot:
    handler: takeScreenshot.default
